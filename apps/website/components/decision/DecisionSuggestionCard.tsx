import Link from "next/link";import {ArrowRight} from "lucide-react";import {Badge,Card} from "@datastorified/ui";import type {DecisionConfig} from "@datastorified/decision-engine";
export function DecisionSuggestionCard({decision,confidence}:{decision:DecisionConfig;confidence?:number}){return <Link href={`/decision/${decision.id}`} className="group block h-full"><Card className="h-full p-5 transition group-hover:-translate-y-1 group-hover:border-primary/20 group-hover:shadow-lift"><div className="flex items-start justify-between gap-3"><Badge>{decision.category}</Badge>{confidence!==undefined&&<span className="text-xs font-bold text-primary">{Math.round(confidence*100)}% match</span>}</div><h3 className="mt-4 text-lg font-bold">{decision.title}</h3><p className="mt-2 text-sm leading-6 text-muted">{decision.description}</p><span className="mt-4 inline-flex items-center gap-1 text-sm font-bold text-primary">Start analysis <ArrowRight size={15}/></span></Card></Link>}
