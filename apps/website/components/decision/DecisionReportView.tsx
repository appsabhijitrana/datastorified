import type {DecisionReport} from "@datastorified/decision-engine";
export function DecisionReportView({report}:{report:DecisionReport}){return <article className="print:block"><h1 className="text-3xl font-bold">{report.title}</h1><p className="mt-2 text-muted">{report.question}</p><p className="mt-5 text-4xl font-bold text-primary">{report.score.score}/100</p><p className="font-semibold">{report.score.label}</p><h2 className="mt-8 text-xl font-bold">Inputs</h2><dl className="mt-3 grid gap-2 sm:grid-cols-2">{report.inputs.map(input=><div key={input.label} className="rounded-xl bg-soft p-3"><dt className="text-xs text-muted">{input.label}</dt><dd className="mt-1 font-semibold">{input.value}</dd></div>)}</dl><h2 className="mt-8 text-xl font-bold">Recommendation</h2><p className="mt-2 font-semibold">{report.recommendation.verdict}</p><p className="mt-2 text-muted">{report.recommendation.summary}</p><h2 className="mt-8 text-xl font-bold">Action plan</h2><ol className="mt-3 list-decimal space-y-2 pl-5">{report.actionPlan.map(item=><li key={item}>{item}</li>)}</ol><p className="mt-8 border-t border-border pt-5 text-xs leading-5 text-muted">{report.disclaimer}</p></article>}
