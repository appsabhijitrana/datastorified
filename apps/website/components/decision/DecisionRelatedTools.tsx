"use client";
import {ArrowUpRight,Calculator,Wrench} from "lucide-react";import {Card} from "@datastorified/ui";import type {DecisionConfig} from "@datastorified/decision-engine";import {trackDecisionEvent} from "@datastorified/analytics";
const title=(slug:string)=>slug.replace(/-calculator$/u,"").split("-").map(x=>x.charAt(0).toUpperCase()+x.slice(1)).join(" ");
export function DecisionRelatedTools({config}:{config:DecisionConfig}){return <div className="grid gap-4 sm:grid-cols-2"><Card className="p-5"><div className="flex items-center gap-2"><Calculator className="text-primary"/><h2 className="font-bold">Related calculators</h2></div><div className="mt-4 space-y-2">{config.relatedCalculators.map(slug=><a key={slug} onClick={()=>trackDecisionEvent("decision_related_calculator_clicked",{decision_id:config.id,calculator:slug})} href={`https://calculators.datastorified.com/${slug}`} className="flex items-center justify-between rounded-xl bg-soft px-3 py-3 text-sm font-semibold hover:text-primary">{title(slug)}<ArrowUpRight size={15}/></a>)}</div></Card><Card className="p-5"><div className="flex items-center gap-2"><Wrench className="text-accent"/><h2 className="font-bold">Useful tools</h2></div><div className="mt-4 space-y-2">{config.relatedTools.map(slug=><a key={slug} href={`https://tools.datastorified.com/${slug}`} className="flex items-center justify-between rounded-xl bg-soft px-3 py-3 text-sm font-semibold hover:text-primary">{title(slug)}<ArrowUpRight size={15}/></a>)}</div></Card></div>}
