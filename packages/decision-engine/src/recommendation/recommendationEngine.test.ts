import {describe,expect,it} from "vitest";import {analyzeDecision} from "../analysis";import {defaultAnswers} from "../questions/questionEngine";import {decisionById} from "../registry/decisions";
describe("recommendation engine",()=>{it("returns verdict and action plan",()=>{const config=decisionById("emergency-fund")!;const recommendation=analyzeDecision(config,defaultAnswers(config)).recommendation;expect(recommendation.verdict).toBeTruthy();expect(recommendation.actionPlan.length).toBeGreaterThan(0);expect(recommendation.nextBestAction).toBe(recommendation.actionPlan[0])})});
