import {describe,expect,it} from "vitest";import {decisions} from "./decisions";
describe("decision registry",()=>{it("contains eight complete unique flows",()=>{expect(decisions).toHaveLength(8);expect(new Set(decisions.map(x=>x.id)).size).toBe(8);for(const decision of decisions){expect(decision.questions.length).toBeGreaterThanOrEqual(6);expect(decision.rules.length).toBeGreaterThanOrEqual(4);expect(decision.factors.length).toBeGreaterThanOrEqual(4);expect(decision.recommendations.length).toBe(5);expect(decision.relatedCalculators.length).toBeGreaterThan(0);expect(decision.scenarios.length).toBeGreaterThan(0)}})});
