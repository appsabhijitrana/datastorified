import {AlertTriangle,CheckCircle2} from "lucide-react";import {Card} from "@datastorified/ui";import type {Risk} from "@datastorified/decision-engine";
export function DecisionRiskCard({risk}:{risk?:Risk}){return <Card className="p-4">{risk?<div className="flex gap-3"><AlertTriangle className={risk.severity==="high"?"text-danger":"text-warning"} size={20}/><div><h3 className="font-bold">{risk.label}</h3><p className="mt-1 text-sm leading-6 text-muted">{risk.message}</p></div></div>:<div className="flex gap-3"><CheckCircle2 className="text-success"/><div><h3 className="font-bold">No critical risk triggered</h3><p className="mt-1 text-sm text-muted">Keep testing conservative scenarios before acting.</p></div></div>}</Card>}
