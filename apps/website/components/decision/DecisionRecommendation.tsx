import {Sparkles} from "lucide-react";import {Card} from "@datastorified/ui";import type {Recommendation} from "@datastorified/decision-engine";
export function DecisionRecommendation({recommendation}:{recommendation:Recommendation}){return <Card className="border-primary/15 bg-gradient-to-br from-primary/[.06] to-accent/[.08] p-6"><div className="flex gap-3"><span className="grid size-11 shrink-0 place-items-center rounded-2xl bg-gradient-to-br from-primary to-accent text-white"><Sparkles size={19}/></span><div><p className="text-xs font-bold uppercase tracking-[.14em] text-primary">Recommendation</p><h2 className="mt-1 text-2xl font-bold">{recommendation.verdict}</h2></div></div><p className="mt-4 leading-7 text-muted">{recommendation.summary}</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><div><p className="font-bold text-success">What supports it</p><ul className="mt-2 space-y-2 text-sm text-muted">{recommendation.pros.map(x=><li key={x}>• {x}</li>)}</ul></div><div><p className="font-bold text-warning">What to watch</p><ul className="mt-2 space-y-2 text-sm text-muted">{recommendation.cons.map(x=><li key={x}>• {x}</li>)}</ul></div></div></Card>}
