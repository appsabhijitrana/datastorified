"use client";
import {useEffect,useState} from "react";import Link from "next/link";import {ArrowRight,Clock3} from "lucide-react";import {Card} from "@datastorified/ui";import {decisionStorage,type StoredDecision} from "@datastorified/decision-engine";
export function DecisionRecent(){const[items,setItems]=useState<StoredDecision[]>([]);useEffect(()=>setItems(decisionStorage.listRecent().slice(0,4)),[]);if(!items.length)return null;return <section className="mx-auto max-w-7xl px-4 pb-16 sm:px-6"><div className="flex items-center gap-2"><Clock3 className="text-primary"/><h2 className="text-2xl font-bold">Continue a recent decision</h2></div><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{items.map(item=><Link key={item.id} href={`/decision/result/${item.id}`}><Card className="h-full p-4 transition hover:-translate-y-1 hover:shadow-lift"><p className="text-xs font-bold uppercase tracking-wide text-primary">{item.analysis.score.score}/100 · {item.analysis.score.label}</p><h3 className="mt-2 font-bold">{item.analysis.config.title}</h3><span className="mt-4 inline-flex items-center gap-1 text-sm font-semibold text-muted">Open result <ArrowRight size={14}/></span></Card></Link>)}</div></section>}
