import { formatDecisionValue, type DecisionReport, type DecisionWorkflow } from "@datastorified/decision-os";

export function DecisionReportView({ report, workflow }: { report: DecisionReport; workflow: DecisionWorkflow }) {
  return <article className="print:block"><h1 className="text-3xl font-bold">{workflow.title}</h1><p className="mt-2 text-muted">Generated {new Date(report.generatedAt).toLocaleString("en-IN")}</p><p className="mt-5 text-4xl font-bold text-primary">{Math.round(report.score.value)}/100</p><p className="font-semibold">{report.score.label}</p><h2 className="mt-8 text-xl font-bold">Inputs</h2><dl className="mt-3 grid gap-2 sm:grid-cols-2">{workflow.questions.map((question) => <div key={question.id} className="rounded-xl bg-soft p-3"><dt className="text-xs text-muted">{question.prompt}</dt><dd className="mt-1 font-semibold">{formatDecisionValue(report.answers[question.id])}</dd></div>)}</dl>{report.recommendation && <><h2 className="mt-8 text-xl font-bold">Recommendation</h2><p className="mt-2 font-semibold">{report.recommendation.title}</p><p className="mt-2 text-muted">{report.recommendation.summary}</p></>}<h2 className="mt-8 text-xl font-bold">Action plan</h2><ol className="mt-3 list-decimal space-y-2 pl-5">{report.actionPlan.map((item) => <li key={item}>{item}</li>)}</ol><p className="mt-8 border-t border-border pt-5 text-xs leading-5 text-muted">This structured educational report is not financial, investment, legal, career, or professional advice. Verify important assumptions independently.</p></article>;
}
