export function DecisionProgress({ value }: { value: number }) { return <div aria-label={`Decision progress ${value}%`}><div className="flex items-center justify-between text-xs font-semibold text-muted"><span>Decision profile</span><span>{value}% complete</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-primary/10"><div className="h-full rounded-full bg-gradient-to-r from-primary to-accent transition-[width]" style={{ width: `${value}%` }} /></div></div>; }
