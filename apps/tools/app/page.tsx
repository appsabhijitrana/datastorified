"use client";
import {useEffect,useMemo,useState} from "react";import {Sparkles,UserRound,Wrench} from "lucide-react";import {tools,searchTools} from "@datastorified/tools-engine/registry";import {storage} from "@datastorified/storage";import {trackFavorite,trackSearch} from "@datastorified/analytics";import {Badge,BottomNav,Button,Card,CategorySection,EmptyState,Footer,Header,SearchBox,ToolCard} from "@datastorified/ui";
export default function Home(){const[q,setQ]=useState("");const[recent,setRecent]=useState<string[]>([]);const[favs,setFavs]=useState<string[]>([]);const[searches,setSearches]=useState<string[]>([]);useEffect(()=>{setRecent(storage.getRecent("tools"));setFavs(storage.getFavorites("tools"));setSearches(storage.getSearches())},[]);const found=useMemo(()=>searchTools(q),[q]);const submitSearch=()=>{const query=q.trim();if(query.length<2)return;storage.addSearch(query);trackSearch(query,"tools");setSearches(storage.getSearches())};const toggle=(slug:string)=>{const active=storage.toggleFavorite("tools",slug);trackFavorite(slug,"tool",active);setFavs(storage.getFavorites("tools"))};const sections=[...new Set(tools.map(x=>x.category))];return <><Header surface="tools"/><main><section className="hero-grid border-b border-border"><div className="mx-auto max-w-4xl px-4 py-14 text-center sm:px-6 sm:py-20"><Badge><Sparkles size={13} className="mr-1"/> Useful, fast, and pleasantly quiet</Badge><h1 className="mt-5 text-balance text-4xl font-bold tracking-[-.04em] sm:text-6xl">Everyday tools without the <span className="bg-gradient-to-r from-primary to-accent bg-clip-text text-transparent">internet clutter</span></h1><p className="mx-auto mt-4 max-w-2xl text-lg leading-8 text-muted">Quick utilities for text, code, files, and small jobs. No account required.</p><div id="search" className="mx-auto mt-8 max-w-2xl"><SearchBox large value={q} onChange={setQ} onSubmit={submitSearch} placeholder="Search JSON, PDF, password, text…"/>{!q&&searches.length>0&&<div className="mt-3 flex flex-wrap justify-center gap-2">{searches.slice(0,5).map(x=><button key={x} onClick={()=>setQ(x)} className="rounded-full border border-border bg-white/80 px-3 py-1.5 text-sm text-muted transition hover:border-primary/30 hover:text-primary">{x}</button>)}</div>}</div></div></section><div className="mx-auto max-w-7xl space-y-16 px-4 py-14 sm:px-6">{q?<CategorySection title={`${found.length} result${found.length===1?"":"s"} for “${q}”`}>{found.length?<Grid items={found} favs={favs} toggle={toggle}/>:<EmptyState title="No tool found"/>}</CategorySection>:<><CategorySection title="Popular tools" description="Fast answers for common small jobs"><Grid items={tools.filter(x=>x.popular)} favs={favs} toggle={toggle}/></CategorySection>{recent.length>0&&<CategorySection id="recent" title="Recently used"><Grid items={recent.map(s=>tools.find(x=>x.slug===s)).filter(Boolean) as typeof tools} favs={favs} toggle={toggle}/></CategorySection>}{favs.length>0&&<CategorySection title="Your favorites"><Grid items={favs.map(s=>tools.find(x=>x.slug===s)).filter(Boolean) as typeof tools} favs={favs} toggle={toggle}/></CategorySection>}<div id="categories" className="space-y-16">{sections.map(cat=><CategorySection key={cat} title={cat}><Grid items={tools.filter(x=>x.category===cat)} favs={favs} toggle={toggle}/></CategorySection>)}</div></>}<section id="profile" className="scroll-mt-24"><Card className="flex flex-col items-start justify-between gap-5 bg-gradient-to-br from-primary/[.05] to-accent/[.07] p-6 sm:flex-row sm:items-center sm:p-8"><div className="flex gap-4"><span className="grid size-11 shrink-0 place-items-center rounded-2xl bg-white text-primary shadow-soft"><UserRound size={20}/></span><div><h2 className="text-lg font-bold">Private utility workspace</h2><p className="mt-1 max-w-2xl text-sm leading-6 text-muted">Favorites and recent tools stay in this browser. Cloud sync and saved dashboards are future Pro features.</p></div></div><Button variant="secondary" disabled>Sign in to sync · Coming later</Button></Card></section></div></main><Footer/><BottomNav/></>}
function Grid({items,favs,toggle}:{items:typeof tools;favs:string[];toggle:(s:string)=>void}){return <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{items.map(x=><ToolCard key={x.slug} name={x.name} description={x.description} category={x.category} href={`/${x.slug}`} favorite={favs.includes(x.slug)} onFavorite={()=>toggle(x.slug)} icon={<Wrench size={20}/>}/>)}</div>}
